1.两数之和

给：一数组，一目标

要求：a.在数组中找到两个值的和为目标值的数

​	   b.返回这两个数的下标，以数组的形式



想法：两个for循环嵌套，一个控制一个数，在嵌套最后进行两数相加，若达到目标数则返回下标，否则继续循环直到结束（注意不能自己加自己）



遇到的错误：最大下标搞错了，循环次数n减一

​		       嵌套循环，上下两个临时变量之间是有联系的

2.字母异位词分组

输入多个字符串，若字符串中字符相同组成一组，否则单独一组